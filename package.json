{
  "name": "proposal-app",
  "version": "1.0.0",
  "description": "A romantic proposal app with love letter and interactive proposal",
  "private": true,
  "scripts": {
    "build": "cd frontend && npm run build"
  },
  "dependencies": {
    "nodemailer": "^6.9.7"
  }
}
